<div class="navbar-smartphone">
  <div class="navbar-home">
    <% css_class_flat = controller?('flats') ? "primary-link" : "secondary-link" %>
    <%= link_to flat_path(@flat) do %>
      <i class="fas fa-home #{css_class_flat}"></i>
    <% end %>
    <p>Home</p>
  </div>
  <div class="navbar-bills">
    <%= link_to flat_bills_path(@flat, @bill) do %>
      <i class="fas fa-file-invoice secondary-link"></i>
    <% end %>
    <p>Bills</p>
  </div>
  <div class="navbar-tasks">
      <%= link_to flat_areas_path(@flat, @area) do %>
    <i class="fas fa-tasks secondary-link"></i>
    <% end %>
    <p>Tasks</p>
  </div>
  <div class="navbar-events">
    <i class="far fa-calendar-alt"></i>
    <p>Events</p>
  </div>
  <div class="navbar-chat">
    <i class="far fa-comments"></i>
    <p>Chat</p>
  </div>
</div>


<% css_class_flat = controller?('flats') ? "nav-link text-orange" : "nav-link text-gray" %>
          <%= link_to "Home", flat_path(@flat), class: "#{css_class_flat}" %>
